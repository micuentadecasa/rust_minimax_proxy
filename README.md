# rust_minimax_proxy
create a proxy for MiniMax CLI to use with agents for not using api keys use services, but code plans
the idea is that i want to use this proxy as a wrapper, so i will have a react app or similar that will do answers to the proxy for llm queries, i will need a .sh script that launches first the proxy, if it is not authenticated will redirect to atuthentication and 